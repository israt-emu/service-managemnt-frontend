/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["i.ibb.co", "source.unsplash.com"], //Domain of image host
  },
};

module.exports = nextConfig;
